package com.reconciliation.common.enums;

public enum TransactionStatus {
    AUTHORIZED,
    CAPTURED,
    FAILED,
    REFUNDED,
    PARTIALLY_REFUNDED,
    DISPUTED,
    CANCELLED,
    PENDING,
    PENDING_SETTLEMENT
}
